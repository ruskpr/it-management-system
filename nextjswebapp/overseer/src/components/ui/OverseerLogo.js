export default function OverseerLogo({ className }) {
  return (
    <span className={`font-extrabold leading-tighter tracking-tighter bg-clip-text text-transparent bg-gradient-to-r from-blue-500 to-teal-400 ${className}`}>
      Overseer
    </span>
  );
}
